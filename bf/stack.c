#include "monty.h"

/**
 * f_stack - sets the format of the data to a stack (LIFO)
 * @head: stack head
 * @counter: line number
 * Return: no return
 */

void f_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
}
